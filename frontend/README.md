# SnapNote Web

SnapNote 的 Vinext / React 前端。完整项目说明、启动方式和 API 配置见仓库根目录 `README.md`。

```powershell
npm.cmd install
npm.cmd run dev
```

未设置 `NEXT_PUBLIC_API_BASE_URL` 时，页面使用浏览器本地演示模式；连接 FastAPI 时请参考 `.env.example`。
